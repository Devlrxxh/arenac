# arenac

Arena and slab allocators for C — fast, scoped memory allocation as an
alternative to `malloc`/`free` for batch allocation patterns.

<!-- TODO: one-line code example, benchmark table, API reference,
     when-to-use / when-not-to, build instructions -->
